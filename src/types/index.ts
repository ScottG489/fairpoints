export interface Topic {
  id: string
  name: string
}

export interface Viewpoint {
  id: string
  name: string
}

export interface Message {
  id: string
  author: string
  body: string
}

export interface Channel {
  id: string
}
